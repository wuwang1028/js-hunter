# JS Hunter Desktop

桌面版JavaScript分析工具，基于Electron开发。

## 功能特性

### 核心功能
- 📁 **批量导入**：支持单文件、多文件、文件夹导入
- 🤖 **AI分析**：复用插件版的AI分析引擎
- 💾 **本地存储**：使用SQLite本地数据库
- 📊 **数据导出**：支持JSON、HTML、Markdown格式

### 优势
- 🖥️ 独立运行，无需浏览器
- 📁 批量处理JS文件
- 💪 更强大的功能
- 🎨 更丰富的界面

## 开发

### 安装依赖

```bash
cd desktop-app
npm install
```

### 开发模式

```bash
npm run dev
```

### 构建

```bash
# 构建所有平台
npm run package

# 仅构建Windows
npm run package:win

# 仅构建macOS
npm run package:mac

# 仅构建Linux
npm run package:linux
```

## 打包

构建完成后，可执行文件位于 `release/` 目录：

- **Windows**: `JS Hunter Setup.exe` 或 `JS Hunter.exe` (portable)
- **macOS**: `JS Hunter.dmg`
- **Linux**: `JS Hunter.AppImage` 或 `js-hunter.deb`

## 技术栈

- **Electron**: 桌面应用框架
- **React**: 前端框架
- **TypeScript**: 类型安全
- **SQLite**: 本地数据库
- **Vite**: 构建工具

## 项目结构

```
desktop-app/
├── src/
│   ├── main/              # 主进程
│   │   ├── main.ts        # 入口文件
│   │   ├── preload.ts     # 预加载脚本
│   │   ├── database.ts    # 数据库管理
│   │   ├── ai-analyzer-wrapper.ts  # AI分析器
│   │   └── file-manager.ts  # 文件管理
│   ├── renderer/          # 渲染进程（React应用）
│   └── shared/            # 共享代码
│       ├── types/         # 类型定义
│       ├── utils.ts       # 工具函数
│       └── analysis-templates.ts  # 分析模板
├── public/                # 静态资源
├── package.json
└── README.md
```

## 注意事项

1. **API密钥**：首次运行需要配置AI API密钥
2. **文件大小**：建议单个文件不超过10MB
3. **并发分析**：默认同时分析2个文件，可在设置中调整

## 许可证

MIT License
